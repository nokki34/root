# neovim
~/.config/nvim

# tmux
~/.tmux.conf

# tmuxinator
~/.config/tmuxinator

# aerospace (macOS-only window manager)
~/.aerospace.toml    @mac

# kitty (arch only; mac uses wezterm)
~/.config/kitty    @arch

# wezterm
~/.wezterm.lua

# zsh
~/.zshrc

# fd
~/.fd.zsh
